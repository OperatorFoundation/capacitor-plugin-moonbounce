import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.plugin.moonbounce',
  appName: 'capacitor-plugin-moonbounce',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
